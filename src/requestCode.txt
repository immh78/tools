v-router를 사용중이다. 

base_url = '/vite-project/'

const router = createRouter({
  history: createWebHashHistory('/vite-project/'), // hash 모드 사용
  routes
})

각 page별로 별도으 manifest 파일을 적용하여 pwa를 사용하여 각 page별로 icon과 app name을 다르게 적용하려면?
